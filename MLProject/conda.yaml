name: workflow-ci-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12.7
  - pip
  - pip:
      - mlflow
      - scikit-learn
      - pandas
      - matplotlib
      - seaborn
      - shap
      - joblib